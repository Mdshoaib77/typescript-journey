function greet(person: string): string {
    return `Assalamuoyalaikum ${person}, Wellcome to my typescript journey`;
}

const username: string = "Alhamdulliah Learning Ts";
console.log(greet(username));
